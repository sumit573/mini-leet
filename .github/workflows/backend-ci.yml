name: Backend CI

on:
  push:
    branches: [ 'main', 'develop', 'feature/*', 'bugfix/*' ]
  pull_request:
    branches: [ 'main', 'develop' ]

jobs:
  backend-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Maven local repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      - name: Build and run tests
        run: mvn -B -DskipTests=false test

# Optional: SonarCloud analysis (requires SONAR_TOKEN secret and project/org configured)
# To enable, uncomment the step below and add SONAR_TOKEN secret in GitHub repository settings.
#      - name: SonarCloud Scan
#        uses: SonarSource/sonarqube-scan-action@v5.0.0
#        env:
#          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
#        with:
#          args: >-
#            -Dsonar.projectKey=sumit573_mini-leet
#            -Dsonar.organization=sumit573
#            -Dsonar.java.binaries=target/classes

